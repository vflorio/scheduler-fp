---
slug: /
sidebar_position: 1
---

# Supervisor

```mermaid
---
config:
  flowchart:
    nodeSpacing: 30
    rankSpacing: 60
---
graph TD
    Service["Bun Service"]
    WebApp["Web App"]

    Service --> TRPC["tRPC Server"]
    WebApp --> TRPC

    TRPC --> AB["Android Bridge"]
    TRPC --> MD["mDNS Discovery"]
    TRPC --> DR["Device Registry"]
    TRPC --> NT["Notification"]

    AB -. implements .-> adb["adb (process)"]
    MD -. implements .-> avahi["avahi (process)"]
    DR -. implements .-> suitest["suitest (HTTP)"]
    NT -. implements .-> slack["slack (HTTP)"]

    DR -- populates --> DB["Internal Device DB"]

    style Service fill:#4a90d9,color:#fff
    style WebApp fill:#4a90d9,color:#fff
    style TRPC fill:#9b59b6,color:#fff
    style AB fill:#f5a623,color:#fff
    style MD fill:#f5a623,color:#fff
    style DR fill:#f5a623,color:#fff
    style NT fill:#f5a623,color:#fff
    style adb fill:#7b8a8b,color:#fff
    style avahi fill:#7b8a8b,color:#fff
    style suitest fill:#7b8a8b,color:#fff
    style slack fill:#7b8a8b,color:#fff
    style DB fill:#2ecc71,color:#fff
```

| Interface | Implements | Purpose |
|---|---|---|
| [**AndroidBridge**](android-bridge) | `adb` (process) | Communicate with Android devices |
| [**mDNS Discovery**](mdns-discovery) | `avahi` (process) | Discover devices on the local network |
| [**Device Registry**](device-registry) | `suitest` (HTTP) | Fetch registered lab devices, populate the internal Device DB |
| [**Notification**](notifications) | `slack` (HTTP) | Send alerts and status notifications |

---

```mermaid
graph TD
    subgraph "@supervisor/trpc (shared)"
        Router["AppRouter<br/>(services routers)"]
        Result["ApiResult"]
        Context["Services interface"]
    end

    subgraph "apps/service (Bun)"
        TRPC_Server["tRPC HTTP Server<br/>(Bun.serve)"]
        Auth["Auth middleware<br/>(localhost whitelist)"]
        Log["Logging middleware"]
        Wire["Service wiring<br/>(ShellAndroidBridge)"]
    end

    subgraph "apps/web-app"
        Client["tRPC Client"]
        Proxy["Vite proxy<br/>(/api/trpc -> service)"]
    end

    TRPC_Server --> Router
    Wire --> Context
    Client -.->|"AppRouter type"| Router
    Proxy -->|"HTTP"| TRPC_Server
```